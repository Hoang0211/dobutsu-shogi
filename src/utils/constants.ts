export enum Side {
  white = 'white',
  black = 'black',
}

export enum Winner {
  white = 'white',
  black = 'black',
  draw = 'draw',
}

export enum PieceName {
  chick = 'chick',
  hen = 'hen',
  elephant = 'elephant',
  giraffe = 'giraffe',
  lion = 'lion',
}

export enum MoveType {
  move = 'move',
  atk = 'atk',
  rev = 'rev',
}

export enum Direction {
  up,
  right,
  down,
  left,
  upright,
  downright,
  downleft,
  upleft,
}
